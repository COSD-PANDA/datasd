dependencies:
  override:
    - bundle install
general:
  branches:
    only:
      - master
      - production
deployment:
  staging:
    branch: master
    commands:
      - gulp publish
#      - surge --project ./site --domain datasd-stage.surge.sh
      - s3_website push
  production:
    branch: production
    commands:
      - gulp publish
      - s3_website push
